import { LazyImageLoaderPipe } from './lazy-image-loader.pipe';

describe('LazyImageLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new LazyImageLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
